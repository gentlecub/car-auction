# ============================================
# Railway Environment Variables
# Copy these to Railway Dashboard for each service
# ============================================

# ============================================
# BACKEND SERVICE VARIABLES
# ============================================

# Database (MySQL - Railway Plugin)
# Railway auto-generates these when you add MySQL plugin:
# MYSQL_URL=mysql://user:password@host:port/database
# Use this format for connection string:
ConnectionStrings__DefaultConnection=Server=${MYSQL_HOST};Port=${MYSQL_PORT};Database=${MYSQL_DATABASE};User=${MYSQL_USER};Password=${MYSQL_PASSWORD};

# Redis (Railway Plugin)
# Railway auto-generates REDIS_URL when you add Redis plugin
# Format: redis://default:password@host:port
Redis__ConnectionString=${REDIS_URL}

# JWT Configuration (REQUIRED - Generate secure keys!)
JwtSettings__SecretKey=YourSuperSecretKeyAtLeast32CharactersLong!
JwtSettings__Issuer=CarAuction
JwtSettings__Audience=CarAuctionClient
JwtSettings__AccessTokenExpirationMinutes=60
JwtSettings__RefreshTokenExpirationDays=7

# CORS - Allow frontend domain (get from Railway after deploying frontend)
Cors__Origins__0=https://your-frontend.up.railway.app

# Enable Swagger for demo
ENABLE_SWAGGER_IN_PRODUCTION=true

# ASP.NET Core
ASPNETCORE_ENVIRONMENT=Production

# Logging
Logging__LogLevel__Default=Warning
Logging__LogLevel__Microsoft=Warning

# ============================================
# FRONTEND SERVICE VARIABLES (Build Args)
# ============================================

# API URL (get from Railway after deploying backend)
VITE_API_URL=https://your-backend.up.railway.app/api

# WebSocket URL for SignalR
VITE_WS_URL=wss://your-backend.up.railway.app/hubs/auction

# Environment
VITE_ENV=production
